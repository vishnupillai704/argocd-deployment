---
# Source: processor/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: processor-canary
  labels:
    app: processor
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: processor
---
# Source: processor/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: processor
  labels:
    app: processor
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: processor
